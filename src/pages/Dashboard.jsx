function Dashboard() {
  return (
    <main className="dashboard">
      <div className="container">
        <h1>User Management Dashboard</h1>
        <p>Manage users efficiently with search, filtering, sorting, pagination, and CRUD operations.</p>
      </div>
    </main>
  );
}

export default Dashboard;